<Page x:Class="WinZ.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:s="clr-namespace:WinZ.Services"
      Background="#1A1A1A" Title="About WinZ"
      KeepAlive="False">
    <Page.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid SnapsToDevicePixels="true" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.Thumb>
                                    <Thumb BorderThickness="0">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#3A3A3A" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource Surface2Brush}" BorderThickness="0,0,0,1" Height="44">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{DynamicResource L.About.Title}"
                           FontWeight="SemiBold" FontSize="14"
                           Foreground="{StaticResource PrimaryBrush}"
                           VerticalAlignment="Center" Margin="20,0,0,0"/>
                <Button Grid.Column="1" Click="Back_Click"
                        Width="32" Height="32"
                        Style="{StaticResource GhostBtn}" Padding="8" Margin="0,6,6,6"
                        VerticalAlignment="Center">
                    <Path Fill="{StaticResource SecondaryBrush}" Stretch="Uniform"
                          Data="M20,11V13H8L13.5,18.5L12.08,19.92L4.16,12L12.08,4.08L13.5,5.5L8,11H20Z"/>
                </Button>
            </Grid>
        </Border>

        <!-- Page content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,40,0" FlowDirection="RightToLeft">
            <Grid Margin="40" FlowDirection="LeftToRight">
                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" MaxWidth="550">
                    <TextBlock Text="{DynamicResource L.Home.Title}" FontSize="42" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <TextBlock Text="PRO UTILITY &#x2014; VERSION 1.0.1" FontSize="10" FontWeight="Black" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                    <Border Background="#212121" CornerRadius="12" Padding="30" BorderBrush="#2D2D2D" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource L.About.Sources}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,12"/>
                            <TextBlock s:TextFormatter.FormattedText="L.About.SourcesDesc"
                                       TextWrapping="Wrap" FontSize="13" Foreground="#AAAAAA" LineHeight="20" Margin="0,0,0,25"/>

                            <TextBlock Text="{DynamicResource L.About.Trans}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,12"/>
                            <TextBlock s:TextFormatter.FormattedText="L.About.TransDesc"
                                       TextWrapping="Wrap" FontSize="13" Foreground="#AAAAAA" LineHeight="20"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,40">
                        <TextBlock Text="{DynamicResource L.About.CreatedBy}" FontSize="10" Foreground="#555555" FontWeight="Bold" Margin="0,0,4,0"/>
                        <TextBlock Text="LAZYLENO" FontSize="10" Foreground="{StaticResource SecondaryBrush}" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
