<Window x:Class="WinZ.Views.WinZDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="#1A1A1A" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1"
        SizeToContent="Height" Width="440">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <Path x:Name="IconPath" Fill="{StaticResource AccentBrush}" Width="24" Height="24" Stretch="Uniform" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="TitleLabel" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
        </StackPanel>
        
        <TextBlock Grid.Row="1" x:Name="MessageLabel" TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="12" LineHeight="20" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="SecondaryBtn" Style="{StaticResource GhostBtn}" Click="Secondary_Click" Margin="0,0,12,0" Width="100" Height="34"/>
            <Button x:Name="PrimaryBtn" Style="{StaticResource PrimaryBtn}" Click="Primary_Click" Width="100" Height="34"/>
        </StackPanel>
    </Grid>
</Window>
