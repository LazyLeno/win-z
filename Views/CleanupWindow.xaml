<Window x:Class="WinZ.Views.CleanupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:WinZ.Converters"
        Title="Smart Cleanup" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="60" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <Style x:Key="CleanupCheckBox" TargetType="CheckBox">
            <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="Border" Width="16" Height="16" CornerRadius="3" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <Path x:Name="CheckMark" Data="M1,4 L3,6 L7,1" Stroke="White" StrokeThickness="2" Visibility="Collapsed" Margin="2"/>
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,15" Background="Transparent">
                <TextBlock Text="{DynamicResource L.Cleanup.Title}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock x:Name="SubtitleTxt" Text="{DynamicResource L.Cleanup.Scanning}" Foreground="{StaticResource SecondaryBrush}" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Content Area -->
            <Grid Grid.Row="1">
                <!-- Summary View -->
                <Grid x:Name="SummaryView" Visibility="Collapsed">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="LeftoversList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceBrush}" Margin="0,0,0,4" Padding="10,6" CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Type}" Foreground="{StaticResource MutedBrush}" FontSize="9" FontWeight="Bold" VerticalAlignment="Center" Width="60"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Path}" Foreground="{StaticResource SecondaryBrush}" FontSize="10" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="10,0"/>
                                            <CheckBox Grid.Column="2" IsChecked="{Binding IsChecked}" Style="{StaticResource CleanupCheckBox}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>

                <!-- Progress View -->
                <Grid x:Name="ProgressView" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" Width="300">
                        <ProgressBar x:Name="CleanupProgress" Height="4" Background="{StaticResource Surface2Brush}" Foreground="{StaticResource AccentBrush}" IsIndeterminate="True" BorderThickness="0"/>
                        <TextBlock x:Name="ProgressStatus" Text="{DynamicResource L.Cleanup.Searching}" Foreground="{StaticResource SecondaryBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Footer -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelBtn" Content="{DynamicResource L.Cleanup.Cancel}" Style="{StaticResource GhostBtn}" Width="100" Click="Cancel_Click" Margin="0,0,10,0"/>
                    <Button x:Name="CleanBtn" Content="{DynamicResource L.Cleanup.CleanNow}" Style="{StaticResource PrimaryBtn}" Width="120" Click="Clean_Click" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
