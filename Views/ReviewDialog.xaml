<Window x:Class="WinZ.Views.ReviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Review Selection" Height="400" Width="480"
        MinHeight="300" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="#1A1A1A"
        BorderBrush="#3A3A3A" BorderThickness="1">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="8" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Review Selection" FontSize="22" FontWeight="SemiBold" Foreground="#E0E0E0" Margin="0,0,0,8"/>
        
        <TextBlock Grid.Row="1" x:Name="CountText" Text="You are about to apply the following changes:" 
                   TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,20"/>

        <Border Grid.Row="2" Background="#252525" CornerRadius="4" Padding="12" Margin="0,0,0,20" BorderBrush="#333333" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ReviewList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="0,6"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource GhostBtn}" Click="Cancel_Click" Margin="0,0,12,0" Padding="20,8"/>
            <Button Content="Confirm &amp; Continue" Style="{StaticResource PrimaryBtn}" Click="Confirm_Click" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
